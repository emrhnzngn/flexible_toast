enum NotifyType { success, warning, error }

enum NotifyPosition {
  top, // Notification appears at the top of the screen
  center, // Notification appears in the center of the screen
  bottom, // Notification appears at the bottom of the screen
}
